# Placeholders only — NEVER commit real API keys.
#
# Phase 1 (current): keys are entered per-user in the browser under
# Settings → API Providers and stored in localStorage. These variables are
# reserved for phase 2, when keys move server-side and the provider proxies
# inject them instead of forwarding the client's Authorization header
# (see docs/providers.md → "Phase 2: moving keys server-side").

MUAPI_API_KEY=
KIE_API_KEY=
AGNES_API_KEY=
